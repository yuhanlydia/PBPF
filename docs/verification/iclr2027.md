# PBPF ICLR 2027 delivery record

Date: 2026-09-14

This record distinguishes executed local checks from prospective formal
experiments. No H200 run, external-authority deployment, or formal benchmark
result is claimed.

## Frozen experiment implementation

- The S0--S4 configuration contains 19 explicit cells, exact model and dataset
  revision pins, seeds 1701/1702/1703, matched actor budgets, preregistered
  gates, five causal controls, and 31 one-factor ablation patches.
- The installed local workflow executed `doctor`, `prepare`, `run`,
  `aggregate`, `verify`, and `package` on commit `e5e5fbf`. It completed 15
  stages, 60 actor requests, and 93 verifier suites under one run ID.
- The frozen-source repository suite on that commit reported 619 passed in
  205.10 seconds. Compileall, four shell scripts, three Slurm scripts, and the
  staged diff check also completed successfully.
- Two independent reviews then found fail-closed and determinism gaps. Four
  follow-up commits bind the exact matrix and task counts, runtime dataset
  snapshots, tokenizer contents, deterministic scientific artifacts, and the
  replication-gate evidence chain.

## Follow-up evidence captured before the requested stop

The user asked that further testing stop before final publication. Existing
focused evidence was retained; no post-merge full-suite claim is made.

| Fix | Evidence obtained before stop |
| --- | --- |
| Exact matrix, gates, ablations, and task inventories | 16/16 new regressions, 36/36 CLI tests, 14/14 related tests |
| Runtime dataset snapshot identity | 7/7 regressions and 61/61 related tests |
| Tokenizer snapshot identity | 5/5 regressions, one complete fake-load case, 49 passed with 2 Torch skips; an already-running non-Torch suite completed with 386 passed and 6 skipped |
| Deterministic artifacts and replication provenance | 2/2 provenance cases and one two-fresh-run deterministic-package case |

The four commits were merged cleanly, but the complete suite, compileall, shell
syntax, and end-to-end workflow were not rerun after their combination because
testing was explicitly stopped.

## Paper package

- Default title: *A Failed Test Is Not a Diagnosis: Particle-Belief Program
  Filtering for Code Repair*.
- The staged manuscript previously compiled all 108 title/abstract/introduction
  combinations, rejected all eight invalid selectors, resolved 15 citations,
  and kept the main text to six pages. The default PDF was eight pages including
  references and required disclosure material.
- The final source archive contains 31 members and nine locked snapshots of the
  final configuration/design inputs.
- Archive SHA-256:
  `c9c2a708c869afaba7c7468ba585cc3a1b0bfa32ca395677310f5b1d227a3109`.
- Per the stop instruction, the final post-lock archive was generated once and
  was not rebuilt or recompiled. All seven empirical result macros remain
  explicitly pending.

## Formal execution boundary

The formal launcher is:

```bash
bash scripts/run_iclr.sh --config configs/experiments/iclr_pbpf.yaml --profile slurm_h200x16 --resume
```

It fails closed until the operator supplies the documented H200/Slurm site,
pinned model and dataset snapshots, root-owned public factory/sandbox inputs,
and an independently provisioned evaluator authority with distinct OS identity.
The repository does not substitute the local smoke experiment for that formal
deployment.
